{
    "private": true,
    "type": "module",
    "scripts": {
        "build": "vite build",
        "dev": "vite",
        "start": "concurrently \"npm run dev\" \"php artisan serve --host=0.0.0.0 --port=8080\"",
        "prod": "concurrently \"php artisan storage:link\" \"npm run build\" \"php artisan serve --host=0.0.0.0 --port=8080\"",
        "test": "echo \"Error: no test specified\" && exit 1"
    },
    "devDependencies": {
        "@tailwindcss/vite": "^4.0.0",
        "axios": "^1.7.4",
        "concurrently": "^9.0.1",
        "laravel-mix": "^6.0.49",
        "laravel-vite-plugin": "^1.2.0",
        "tailwindcss": "^4.0.0",
        "vite": "^6.0.11"
    }
}
